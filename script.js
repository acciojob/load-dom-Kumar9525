document.addEventListener("DOMContentLoaded", function(event) {
  // Ensure the body tag is initially empty:
  document.body.innerHTML = "";

  // Write the string to the body:
  document.body.textContent = "DOM load success";
});
